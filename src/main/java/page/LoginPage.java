package page;

import org.openqa.selenium.WebDriver;

public class LoginPage {
	
	
	WebDriver driver;//initializing the driver 
	
	public LoginPage(WebDriver driver) {
		this.driver = driver; // the constructor to invite the driver
	}

}